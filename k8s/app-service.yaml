apiVersion: v1
kind: Service
metadata:
  name: gym-catalog-service
  namespace: gym-dev
  labels:
    app: gym-catalog-service
spec:
  type: ClusterIP
  ports:
    - port: 4005
      targetPort: 4005
      protocol: TCP
      name: http
  selector:
    app: gym-catalog-service
