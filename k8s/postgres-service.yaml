apiVersion: v1
kind: Service
metadata:
  name: gym-catalog-postgres
  namespace: gym-dev
  labels:
    app: gym-catalog-postgres
    component: database
    service: gym-catalog-postgres
spec:
  type: ClusterIP
  ports:
    - port: 15432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: gym-catalog-postgres
    component: database
